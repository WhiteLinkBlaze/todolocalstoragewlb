# AGENTS.md

## Communication

- 기본 답변은 한국어로 작성한다.
- 작업 내용은 짧고 명확하게 공유한다.
- 사용자가 요청하지 않은 큰 구조 변경은 먼저 의도를 확인한다.

## Workflow

- 작업 전 현재 브랜치와 `git status`를 확인한다.
- 사용자 변경사항을 되돌리지 않는다.
- 변경 범위는 요청과 직접 관련된 파일로 제한한다.
- 파일 수정 후 가능한 검증 명령을 실행한다.
- 검증을 실행하지 못한 경우 그 이유를 명확히 남긴다.

## Project

- 이 프로젝트는 의존성 없는 정적 Todo 앱이다.
- 별도 빌드 도구 없이 `index.html`, `styles.css`, `src/app.js` 중심으로 작업한다.
- Todo 데이터는 브라우저 `localStorage`에 저장한다.
- 접근성과 모바일 화면 동작을 함께 고려한다.

## Style

- HTML, CSS, JavaScript는 기존 파일의 스타일을 따른다.
- 불필요한 라이브러리나 프레임워크를 추가하지 않는다.
- CSS는 반응형 레이아웃과 텍스트 넘침을 고려해 작성한다.
- 주석은 코드만으로 의도가 불분명한 곳에만 짧게 남긴다.
